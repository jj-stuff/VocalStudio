# Dev-only seed audio for DevSeedingProjectStore — never commit third-party/copyrighted audio.
/DevAssets/

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store

# Local tool and editor config (any dot-folder at any level) and agent skill lockfiles
.*/
!.github/
skills-lock.json
AGENTS.md
